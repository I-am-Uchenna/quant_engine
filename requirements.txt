# Runtime dependencies for the Streamlit dashboard and the pure-Python
# reference pricing engine. This set installs cleanly on Streamlit Community
# Cloud, which does not compile native extensions.
#
# For the full stack (C++ build toolchain, Ray, gRPC, Excel, tests) install
# requirements-dev.txt instead.
numpy>=1.26,<3.0
scipy>=1.13,<2.0
duckdb>=1.5,<2.0
plotly>=6.0,<7.0
streamlit>=1.41,<2.0
